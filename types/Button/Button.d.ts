/// <reference types="react" />
import { ButtonProps } from "./Button.types";
declare const Button: ({ className, onClick, primary, size, text, appearance, children, disabled, iconOnly, ...rest }: ButtonProps) => JSX.Element;
export default Button;
